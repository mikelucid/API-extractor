APP_NAME=Rootv2Server
APP_ENV=local
APP_DEBUG=true
ROOTV2_DATA_DIR=storage/app/rootv2
ROOTV2_IDENTITY_KEY=dev-only-passphrase
# Amorphous fabric never calls live AWS from tests or default boot.
AMORPHOUS_SYNTHESIZER=mock
